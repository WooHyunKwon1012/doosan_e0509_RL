1. doosan e0509 로봇팔을 이용하여 isaacsim 에 rslrl 정책으로 만들어진 pt 파일로 구동하는 python 파일 만드는 것이 목표

2. Rslrl_move_right.py에서 pt 파일 불러오는 방식, 환경 구성을 어떻게 해야되는지를 기반으로 해서 만들기, 이건 nvidia 의 franka 모델로 구동하는 것. rslrl 방식. 

3. 환경에 관한 요소는 e0509_reach_env_cfg.py 를 참고.

3. e0509_jointpos_ros2.py 이건 isaacsim 에서 로봇팔 e0509 를 부르고 ros2 연결하는 코드인데, 여기서 로봇팔 e0509 를 불러오는 방식을 채택해. usd path 는 /home/woo/ros2_ws/src/doosan-robot2/dsr_description2/usd/e0509.usd

4. pt 구조를 알아보는 코드로는 

```import torch

ckpt_path = "/home/woo/isaac_lab_ws/IsaacLab/logs/rsl_rl/franka_reach/2025-11-28_20-03-27/model_999.pt"
ckpt = torch.load(ckpt_path, map_location="cpu")

msd = ckpt["model_state_dict"]
print(type(msd))
print("num keys in model_state_dict:", len(msd))
print("some keys:", list(msd.keys())[:20])
```

를 알고 있음.

5. 더 필요한 정보가 있으면 나한테 요구하도록 해

6. step by step 으로 시작.
